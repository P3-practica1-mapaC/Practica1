project Default is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("main.adb");

   package Prove is
   end Prove;

   package Compiler is
      for Switches ("ada") use ("-gnat12", "-gnata", "-g", "-fstack-check", "-gnato");
   end Compiler;

end Default;

